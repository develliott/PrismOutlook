<UserControl x:Class="PrismOutlook.Modules.Mail.Views.MailList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:ig="http://schemas.infragistics.com/xaml/wpf"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Background="White" Margin="5,0,0,0"></Grid>

        <Grid Grid.Column="1" MinWidth="250" Background="White">
            <ig:XamDataGrid>

            </ig:XamDataGrid>
        </Grid>

        <Border Grid.Column="1" Margin="5,0,0,0" Background="White"></Border>
        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Width="10" HorizontalAlignment="Left" Grid.RowSpan="100"></GridSplitter>

    </Grid>
</UserControl>